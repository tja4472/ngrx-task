{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "noEmit": true,
    "sourceMap": false,
    // Explicit about types included to avoid clashing with Jest global types
    "types": ["cypress", "node", "cypress-firebase"]
  },
  "files": [],
  "include": ["**/*.ts"],
  "exclude": ["node_modules"]
}
